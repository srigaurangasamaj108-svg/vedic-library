# 🧱 PHASE 2 — CANONICAL EXPANSION FOLDER STRUCTURE

## Vedic Library Project

**Phase:** 2
**Status:** AUTHORITATIVE (Phase‑Scoped)
**Category:** ARCHITECTURAL SPECIFICATION
**Depends on:** Phase‑1 (LOCKED)
**Prepares for:** Phase‑3 (Interpretive Layers)

---

## 0. Purpose of This Document

This document defines the **final, correct, and phase‑aligned folder structure** for **Phase‑2: Canonical Expansion**.

It exists to answer one question only:

> **Where does each class of data, document, and code live during Phase‑2 — and where must it NOT live?**

This document:

* Enforces canonical discipline
* Prevents phase leakage
* Aligns filesystem layout with constitutional and architectural law
* Ensures Phase‑3 can begin without reorganization

This is **not a suggestion**. It is a **binding architectural contract for Phase‑2**.

---

## 1. Phase‑2 Governing Principle

> **Phase‑2 expands canon, not meaning.**

Therefore:

* Canonical text **increases in breadth** (more śāstra)
* Canonical identity **does not change**
* No interpretive, semantic, or pedagogical layers are introduced
* Absence is represented by **absence**, not placeholders

---

## 2. Top‑Level Project Structure (Phase‑2 Valid)

```text
vedic-library-v1.0/
├── data/
├── docs/
├── registry/
├── scripts/
├── frontend/
├── validators/
├── schemas/
└── PROJECT_STATUS.md
```

🔒 **Rules**

* No new top‑level directories are introduced in Phase‑2
* Phase‑2 must not require moving any Phase‑1 files

---

## 3. `data/` — Canonical Corpus Expansion (Phase‑2 Core)

### 3.1 Canonical Principle

All canonical content lives **inside its śāstra path**.

There are:

* ❌ NO global `_canonical/` directories
* ❌ NO global `_extended/` directories
* ❌ NO global `_indexes/` directories

Canonical authority is determined by **path + UID**, never by convenience.

---

### 3.2 Canonical Data Layout (Authoritative)

```text
data/
├── sruti/
│   └── <text>/<section>/<work>/
│       ├── canonical/
│       └── index/
├── smrti/
├── itihasa/
├── purana/
├── agama/
├── vedanga/
├── upaveda/
├── darsana/
└── sampradaya/
```

Each `<work>/` directory MAY contain:

* `canonical/` — canonical units only
* `index/` — machine‑generated verse / unit indexes

Nothing else.

---

### 3.3 Canonical Directory Rules

Inside `canonical/`:

```text
canonical/
├── <uid>.json
├── <uid>.json
└── ...
```

🔒 Rules:

* One file per canonical unit
* Schema‑bound (`canonical-*.schema.md`)
* Immutable once locked
* No translations
* No commentary
* No explanations

---

### 3.4 Index Directory Rules

```text
index/
├── <uid>.index.json
├── <uid>.index.json
└── ...
```

🔒 Rules:

* Generated by pipeline only
* Never hand‑edited
* Contains **references only**, never text
* Safe to regenerate

Indexes are **routing artifacts**, not authority.

---

## 4. `docs/` — Constitutional & Architectural Authority

```text
docs/
├── constitution/
│   ├── PROJECT_CONSTITUTION.md
│   ├── CANONICAL_TEXT_POLICY.md
│   └── CANONICAL_IDENTITY_PRINCIPLES.md
│
├── architecture/
│   ├── UID_SYSTEM.md
│   ├── UID_REFINEMENT_RULES.md
│   ├── CANONICAL_UNIT_DEFINITION.md
│   ├── SCHEMA_OPTIONALITY_RULES.md
│   ├── DATA_SCHEMA.md
│   ├── DATA_LOADING_ABSTRACTION_STRATEGY.md
│   ├── NAMING_CONVENTIONS.md
│   ├── VERSE_REFERENCE_CONTRACT.md
│   ├── CANONICAL_INTAKE_AND_NORMALIZATION.md
│   ├── SEMANTIC_DERIVATION_POLICY.md
│   └── PIPELINE_REGISTRY.md
│
├── schemas/
│   ├── canonical-verse.schema.md
│   ├── canonical-sutra.schema.md
│   ├── canonical-mantra.schema.md
│   ├── canonical-ritual-unit.schema.md
│   └── canonical-lexeme.schema.md
│
├── phases/
│   ├── phase-1/   # LOCKED — historical record
│   └── phase-2/
│       ├── PHASE_2_CANONICAL_EXPANSION_PLAN.md
│       ├── PHASE_2_TASK_CHECKLIST.md
│       ├── PHASE_2_STATUS.md
│       └── PHASE_2_FOLDER_STRUCTURE.md
│
└── ui-ux/
    └── snapshots/
```

🔒 Rules:

* Constitution > Architecture > Schemas > Phases
* Phase documents never rewrite earlier phases

---

## 5. `registry/` — Canonical Identity Law

```text
registry/
├── UID_REGISTRY.json
├── sruti/
├── smrti/
├── itihasa/
├── purana/
├── agama/
├── vedanga/
├── upaveda/
├── darsana/
└── sampradaya/
```

🔒 Rules:

* Registries are **identity law**, not data
* Read‑only inputs to pipelines
* Versioned, justified, immutable once published

---

## 6. `scripts/` and `validators/` — Mechanical Authority Only

```text
scripts/
├── intake/
├── build/
└── utils/

validators/
├── validate_uid.py
├── validate_schema.py
├── validate_canonical_policy.py
└── ...
```

🔒 Rules:

* Scripts are deterministic
* No interpretation
* No heuristics
* No AI decision‑making

---

## 7. `frontend/` — Reader‑Only UI (Phase‑2 Constraint)

```text
frontend/
├── src/
│   ├── app/
│   ├── components/
│   └── lib/
```

🔒 Rules:

* Reader‑first
* Multi‑śāstra navigation allowed
* No commentary rendering
* No overlays
* No personalization

---

## 8. Explicit Phase‑2 Prohibitions

Phase‑2 MUST NOT include:

❌ Commentary directories
❌ Knowledge / concept layers
❌ Educational UX
❌ Search systems
❌ Auth, donations, accounts
❌ AI‑generated canonical content
❌ Placeholder folders for future phases

All of the above belong to **Phase‑3+ only**.

---

## 9. Stability Guarantee

This folder structure is designed so that:

* Phase‑3 adds **new directories**, not rewrites old ones
* Phase‑4+ can scale without migration pain
* A future contributor can understand Phase‑2 in isolation

> **If Phase‑2 folders feel “boring”, they are correct.**

---

## 10. Status Declaration

**PHASE_2_FOLDER_STRUCTURE.md**
**Category:** ARCHITECTURAL CONTRACT
**Applies to:** Phase‑2 only
**Modification:** Only by explicit Phase‑2 amendment

🔒 **LOCKED ON PHASE‑2 COMPLETION**

---

> *Structure is how restraint survives ambition.*
>
> *Phase‑2 builds the shelf before placing more books.*

