# Component Architecture

Atomic Structure:

/components
  /atoms
  /molecules
  /organisms
  /layouts

Canon-agnostic components only.

Never build:
GitaVerseComponent

Always build:
ScripturalUnitComponent